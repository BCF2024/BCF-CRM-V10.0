BearCrest CRM V6.9

- Makes the comp map and one-click PDF controls prominent.
- Adds visible green/yellow/red/gray map legend.
- Keeps excluded comps visible as gray pins while removing them from ARV.
- Recalculates ARV instantly when a comp is checked or unchecked.
- Adds a top One-Click Comp Report PDF button.
- Updates PWA cache to V6.9.

The interactive map uses OpenStreetMap/Leaflet, so no separate Google Maps API key is required.
